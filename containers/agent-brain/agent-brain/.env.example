# Agent Brain Configuration
ZEROCLAW_URL=http://127.0.0.1:3000
ZEROCLAW_SECRET=
LITELLM_URL=http://127.0.0.1:4000/v1
WORKSPACE=/var/www/agentic-website
GIT_REMOTE=origin
HEAVY_MODEL=heavy-coder
FAST_MODEL=fast-agent
VISION_MODEL=vision
AUTONOMY=supervised
SITE_URL=https://daveai.tech

# Security — strong JWT secret (generated 2025)
JWT_SECRET=CHANGE_ME

# Admin
ADMIN_EMAIL=fnice1971@gmail.com

# Anthropic fallback (set real key for Claude fallback)
ANTHROPIC_API_KEY=CHANGE_ME

# GitLab (set after gitlab is running)
GITLAB_URL=http://localhost:8929

# SMTP email alerts (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=